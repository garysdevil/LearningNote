Java：
静态类型语言。
常用数据类型：
数字类型，String，Map，List。
常用设计模式：
工厂模式，观察者模式，装饰模式。
单例（懒汉饿汉）
特性：封装（根据需求暴露接口），继承（复用），多态（根据需求，决定这个人变成女人还是男人）。

Nodejs
动态类型语言
常用函数：
foreach(), filter(), map(), reduce()
函数式编程
声明式，命令式。
promise， async await。


